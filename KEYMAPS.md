# Neovim キーマップ & 使い方ガイド

> このファイルは `/Users/s11641/.config/nvim` の設定を元に生成されています。
> キーマップを更新したら `/update-nvim-docs` スキルで拡充できます。

**Leader キー**: `<Space>`
**LocalLeader キー**: `\`

---

## 基本操作

### ノーマル / ビジュアルモード共通

| キー | 動作 |
|------|------|
| `ww` | ファイル保存 (`:w`) |
| `qq` | 終了 (`:q`) |
| `<S-u>` | Redo |

### カーソル移動

| キー | 動作 | モード |
|------|------|--------|
| `<C-h>` | 左へ単語移動 | Normal / Visual |
| `<C-l>` | 右へ単語移動 | Normal / Visual |
| `<S-h>` | 左へ単語移動 | Normal / Visual |
| `<S-l>` | 右へ単語移動 | Normal / Visual |
| `<C-j>` | 半ページ下 (`<C-d>`) | Normal / Visual |
| `<C-k>` | 半ページ上 (`<C-u>`) | Normal / Visual |

### インサートモード

| キー | 動作 |
|------|------|
| `jj` / `kk` | ノーマルモードへ戻る |
| `<C-h/l/j/k>` | 矢印キー (左/右/下/上) |

---

## ファイルツリー (NvimTree)

| キー | 動作 |
|------|------|
| `<C-E>` | NvimTree 開閉トグル |
| `<C-e>` | NvimTree を開いてカレントファイルにフォーカス |
| `<C-r>` | NvimTree をリフレッシュ |

### NvimTree 内のキー

| キー | 動作 |
|------|------|
| `f` | ライブフィルター開始 |
| `F` | フィルタークリア |
| `H` | 隠しファイル表示トグル |
| `s` | 垂直分割で開く |
| `m` | ノードのリネーム |
| `<C-r>` | ツリーをリロード |

---

## ファジーファインダー (Telescope)

| キー | 動作 |
|------|------|
| `<C-f>` | ファイル検索 |
| `<S-f>` | テキスト全文検索 (live grep) |
| `<C-c>` | コマンドラインパレット |

### Telescope 内のナビゲーション

| キー | 動作 | モード |
|------|------|--------|
| `<C-j>` / `<C-k>` | 選択項目を下/上へ | Insert |
| `jj` | ピッカーを閉じる | Insert |
| `qq` | ピッカーを閉じる | Normal |

---

## LSP

> バッファに LSP がアタッチされると有効になるバッファローカルキーマップ

| キー | 動作 |
|------|------|
| `gd` | 定義へジャンプ |
| `vgd` | 定義を垂直分割で開く |
| `K` | ホバー情報を表示 |
| `<C-m>` | シグネチャヘルプ |
| `gy` | 型定義へジャンプ |
| `gi` | 実装へジャンプ |
| `gr` | 参照一覧を表示 |
| `rn` | シンボルのリネーム |
| `ma` | コードアクション |
| `[d` | 前の診断へ移動 |
| `]d` | 次の診断へ移動 |

---

## 補完 (nvim-cmp)

| キー | 動作 | モード |
|------|------|--------|
| `<C-Space>` | 補完をトリガー | Insert |
| `<TAB>` / `<C-j>` | 次の候補 | Insert |
| `<S-TAB>` / `<C-k>` | 前の候補 | Insert |
| `<CR>` | 補完を確定 | Insert |

---

## Git 操作

| キー | 動作 |
|------|------|
| `<leader>gl` | LazyGit を開く |
| `<leader>lg` | LazyGit をフロートで開く (toggleterm) |
| `<leader>gb` | Git リンクをコピー (Normal / Visual) |
| `<leader>gs` | Git blame (現在行) |
| `<leader>gwl` | Git worktree 一覧 |
| `<leader>gwc` | Git worktree を作成 |

---

## テスト (Neotest)

| キー | 動作 |
|------|------|
| `<leader>tr` | カーソル近傍のテストを実行 |
| `<leader>tf` | カレントファイルのテストを全実行 |
| `<leader>td` | カーソル近傍のテストを DAP でデバッグ |
| `<leader>ts` | テストサマリーパネルをトグル |
| `<leader>to` | テスト出力を表示 |

---

## ターミナル (toggleterm)

| キー | 動作 | モード |
|------|------|--------|
| `<C-\>` | フロートターミナルをトグル | Normal / Insert / Terminal |
| `<leader>tt` | フロートターミナルをトグル | Normal |
| `<leader>th` | 水平ターミナルをトグル | Normal |
| `<leader>tv` | 垂直ターミナルをトグル | Normal |

### ターミナル内のキー

| キー | 動作 |
|------|------|
| `<Esc><Esc>` | ノーマルモードへ戻る |
| `<C-h>` | 左のウィンドウへ移動 |
| `<C-l>` | 右のウィンドウへ移動 |

---

## 診断 / その他ツール

| キー | 動作 |
|------|------|
| `<space>e` | Trouble 診断パネルをトグル |
| `<S-t>` | 選択テキストを翻訳 (Pantran) |
| `<C-t>` | GitHub Copilot 候補を確定 (Insert) |
| `<leader>mt` | Markdown レンダリングをトグル |

---

## ファイルタイプ別

### Go

| キー | 動作 |
|------|------|
| `<S-y>` | ホバー情報を表示 (`K` と同等) |

---

## 設定ファイル早見表

| ファイル | 内容 |
|---------|------|
| `lua/00_init.lua` | 基本設定 (インデント、検索など) |
| `lua/01_keymaps.lua` | グローバルキーマップ |
| `lua/lsp.lua` | LSP 設定 + LSP キーマップ |
| `lua/plugins.lua` | プラグイン仕様 + プラグインキーマップ |
| `ftplugin/` | ファイルタイプ別設定 |
